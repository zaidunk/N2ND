runConfig:
  minInstances: 0
  maxInstances: 2
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Environment variables — set via Firebase Console:
#   NEXT_PUBLIC_API_URL  (optional, empty = use internal API routes)
